
networks:
  shared_network:
    external: true

services:
  nginx:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: trescudo-nginx
    # Remove external port mapping - proxy will handle this
    volumes:
      - ./dist:/usr/share/nginx/html:ro
      - ./conf/nginx.prod.conf:/etc/nginx/nginx.conf:ro
    restart: unless-stopped
    networks:
      - shared_network
    environment:
      - TZ=UTC
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
    healthcheck:
      test: ["CMD", "wget", "--quiet", "--tries=1", "--spider", "http://localhost/"]
      interval: 30s
      timeout: 3s
      start_period: 5s
      retries: 3
